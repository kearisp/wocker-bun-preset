{
    "name": "bun",
    "version": "1.0.2",
    "type": "dockerfile",
    "dockerfile": "./Dockerfile",
    "buildArgsOptions": {
        "IMAGE_VERSION": {
            "message": "Docker image version tag",
            "type": "text",
            "required": true,
            "default": "alpine"
        }
    },
    "volumeOptions": [
        "./:/usr/app"
    ]
}
